 DATABASE_URL=postgresql+asyncpg://rag:rag@localhost:5432/rag
 APP_ENV=dev
 LOG_LEVEL=INFO
 EMBEDDING_PROVIDER=openai
 OPENAI_API_KEY=your-openai-api-key-here

 # Retrieval score threshold (cosine distance 0–2, lower = more similar)
 # Chunks with distance above this value are filtered out as irrelevant.
 # Lower values = stricter relevance. Default: 0.40
 # RETRIEVAL_SCORE_THRESHOLD=0.40
